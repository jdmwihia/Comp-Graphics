[project]
name = "python-opengl"
version = "0.1.0"
description = ""
authors = [
    {name = "NGIYE",email = "georgengiye3@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pandas (>=3.0.2,<4.0.0)",
    "pyopengl (>=3.1.10,<4.0.0)",
    "pyopengl-accelerate (>=3.1.10,<4.0.0)"
]

[tool.poetry]
packages = [{include = "python_opengl", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
